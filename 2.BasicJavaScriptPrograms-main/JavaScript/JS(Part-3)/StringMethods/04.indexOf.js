// String Methods with Arguments:
// Argument is asome value that we pass to the method.

let name = "rajia rani";
let index = name.indexOf("rani");
console.log(index);
let index1 = name.indexOf("z");
console.log(index1);
let index2 = name.indexOf("i");
console.log(index2);