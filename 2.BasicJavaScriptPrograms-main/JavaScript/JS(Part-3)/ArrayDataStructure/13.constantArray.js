// Constant array
// after make the array as a constant we can do all the array methods like push,pop,shift, unshift.
// but we cant assign the array to new array.

const arr = [1 ,2 , 3, 4];
arr.push(5);
arr.unshift(0);
// let newarr = [1,2,3,4];
// arr = newarr;
console.log(arr);
