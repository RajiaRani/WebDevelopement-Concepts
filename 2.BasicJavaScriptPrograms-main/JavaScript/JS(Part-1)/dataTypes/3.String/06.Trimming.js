// The trim() method removes whitespace from both ends of a string.

let extraSpace = "                   Hello, everyone            ";
let removeSpace = extraSpace.trim();
console.log("Removed extra space = ", removeSpace);
