// The replace() method replaces parts of the string with another.

let text = " hello, Miss Rajia Rani";
let replaceText = text.replace("Miss", "Mrs.");
console.log("Ater applied replace property = ", replaceText);